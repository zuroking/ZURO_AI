# Runtime deps. These mirror pyproject.toml; install the package itself with
# `pip install -e .` to get the kronos_synapse CLI. Dev tools live in
# requirements-dev.txt / the [dev] extra.
torch>=2.3.0
tokenizers>=0.19.0
pydantic>=2.7.0
pydantic-settings>=2.3.0
typer>=0.12.0
rich>=13.7.0
numpy>=1.26.0
